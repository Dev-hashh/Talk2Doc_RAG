[project]
name = "talk2doc"
version = "0.1.0"
description = "Local PDF question-answering CLI using embeddings, FAISS, and Ollama"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faiss-cpu",
    "numpy",
    "pypdf>=4.1.0",
    "requests",
    "sentence-transformers",
]
